# Test Environment Configuration
NODE_ENV=test
PORT=5001

# ✅ Use MongoDB Atlas Cloud only (no localhost, no DB name)
MONGODB_URI=mongodb+srv://db_sih:12345@cluster1.uepik1b.mongodb.net/?retryWrites=true&w=majority

# JWT
JWT_SECRET=test_jwt_secret
JWT_EXPIRE=30d
JWT_COOKIE_EXPIRE=30

# File Upload Configuration
MAX_FILE_UPLOAD=1000000
FILE_UPLOAD_PATH=./public/uploads

# Email Configuration (Mocked in tests)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_EMAIL=test@example.com
SMTP_PASSWORD=test123
FROM_EMAIL=noreply@studenthub.com
FROM_NAME=Student Hub

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
